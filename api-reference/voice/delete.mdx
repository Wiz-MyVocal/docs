---
title: "Delete Voice"
api: "DELETE https://api.mintlify.com/sound_clone/api/v1/voices/{voiceId}"
description: "This endpoint deletes an existing user."
---
### Header
<ParamField header="accessKey"   type="string">
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>



### Response

<ResponseField name="code" type="number">
  Indicates whether the call was successful. 1 if successful, 0 if not.
</ResponseField>

<ResponseField name="message" type="string">
  Indicates whether the call was successful. 1 if successful, 0 if not.
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --globoff --request DELETE 'https://api.mintlify.com/sound_clone/api/v1/voices/{voiceId}' \
--header 'Content-Type: application/json' \
--header 'accessKey: xxxx'
```
</RequestExample>

<ResponseExample>

```json Response
{
  "code": 1,
  "message": "Success",
}
```

</ResponseExample>
