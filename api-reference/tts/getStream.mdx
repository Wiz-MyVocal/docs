---
title: "Get TTS History Item"
api: "POST https://api.mintlify.com/sound_clone/sound_clone/api/v1/tts/create"
description: "This endpoint gets or creates a new user."
---
### Header
<ParamField header="accessKey"   type="string" required>
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>

### Body
<ParamField body="voiceId" type="string">
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>
<ParamField body="ttsTitle" type="string">
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>
<ParamField body="ttsText" type="string">
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>
<ParamField body="language" type="string">
  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>
<ParamField body="voiceSettings" type="object">
<Expandable title="Toggle object">
    <ParamField body="stability" type="number">
  This is the current user group token you have for the user group that you want
  to rotate.
    </ParamField>
</Expandable>

  This is the current user group token you have for the user group that you want
  to rotate.
</ParamField>

### Response
responType streaming


<RequestExample>

```bash Example Request
curl -X 'POST' \
'https://api.mintlify.com/sound_clone/sound_clone/api/v1/tts/create' \
--header 'accept: audio/mpeg' \
--header 'accessKey: xxxx'
--header 'Content-Type: application/json' \
--data '{
    "voiceId":"221",
    "ttsTitle":"222",
    "ttsText":"hello word",
    "language":"US_ENGLISH",
    "voiceSettings":{
        "stability":0.7
    }
}'
```
</RequestExample>

<ResponseExample>

```json success
This response does not have an example.
```
```json error 
{
  	"code": -1,
	"message": "error msg",
}
```

</ResponseExample>
